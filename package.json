{
  "name": "dr-ishita-katyal",
  "version": "1.0.0",
  "private": true,
  "description": "Website and content manager for Dr. Ishita Katyal, Plastic, Reconstructive & Aesthetic Surgeon",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "seed": "npm --prefix server run seed",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "start": "NODE_ENV=production npm --prefix server start",
    "test:api": "npm --prefix server run test:smoke"
  }
}
