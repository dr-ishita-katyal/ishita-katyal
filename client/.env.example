# Only needed if the API lives on a different origin to the site.
# Left unset, the client calls /api on its own origin, which is what the Vite
# dev proxy and the production server both expect.
# VITE_API_URL=https://api.example.com/api
